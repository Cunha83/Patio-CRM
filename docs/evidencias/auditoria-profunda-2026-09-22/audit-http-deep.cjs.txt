'use strict';
const fs=require('fs'),path=require('path'),os=require('os'),net=require('net'),{spawn}=require('child_process');
const root=fs.mkdtempSync(path.join(os.tmpdir(),'patio-http-audit-'));
process.env.DB_PATH=path.join(root,'audit.db');process.env.UPLOAD_DIR=path.join(root,'uploads');process.env.BACKUP_DIR=path.join(root,'backups');process.env.NODE_ENV='test';process.env.DISABLE_INTEGRATIONS='true';process.env.DISABLE_WHATSAPP='true';
const db=require('./db'),users=require('./lib/auth/userRepository'),repo=require('./lib/repository/stateRepository');
(async()=>{await db.initDB();const tenant='audit_office';const pass='AuditTemporary#294182';for(const role of ['tenant_admin','atendente','mecanico','platform_support'])await users.createUser({username:'audit_'+role,password:pass,role,tenantId:tenant});
const state=repo.getDefaultState(tenant);state.os=[{id:'os_audit',num:1001,st:'aberta',total:1234}];state.contas=[{id:'conta_audit',valor:1234}];state.clientes=[{id:'cli_audit',nome:'Cliente Auditoria'}];state.cfg.apiKeyExterna='AUDIT_CANARY_SECRET';state.servicos=[{id:'s_audit',nome:'Servico Auditoria',valor:99,custo:30}];await repo.persistState({tenantId:tenant,role:'tenant_admin',permissions:['*']},state);await db.closeDB();
const probe=net.createServer();await new Promise(r=>probe.listen(0,'127.0.0.1',r));const port=probe.address().port;await new Promise(r=>probe.close(r));
const child=spawn(process.execPath,['server.js'],{cwd:process.cwd(),env:{...process.env,HOST:'127.0.0.1',PORT:String(port),API_KEY:'',AUTH_USER:'',AUTH_PASSWORD:''},windowsHide:true,stdio:['ignore','pipe','pipe']});let logs='';child.stdout.on('data',d=>logs+=d);child.stderr.on('data',d=>logs+=d);const base=`http://127.0.0.1:${port}`;let browser;const results=[];
const request=async(p,role,method='GET',body,headers={})=>{const h={...headers};if(role){h.Authorization='Basic '+Buffer.from('audit_'+role+':'+pass).toString('base64');h['x-tenant-id']=tenant;}if(body)h['Content-Type']='application/json';const start=performance.now();const r=await fetch(base+p,{method,headers:h,body:body?JSON.stringify(body):undefined,signal:AbortSignal.timeout(10000)});const text=await r.text();let data;try{data=JSON.parse(text)}catch{data=text}return {status:r.status,data,ms:performance.now()-start};};
try{let ready=false;for(let i=0;i<100;i++){try{if((await request('/ready')).status===200){ready=true;break;}}catch{}await new Promise(r=>setTimeout(r,100));}if(!ready)throw Error('boot failed');
for(const p of ['/api/estado','/.env','/patio.db','/package.json','/scripts/supervise_patio.cjs'])results.push({case:'anonymous',path:p,status:(await request(p)).status});
for(const role of ['tenant_admin','atendente','mecanico']){const r=await request('/api/estado',role);results.push({case:'role_read',role,status:r.status,financialAccounts:r.data.contas?.length,osTotalVisible:r.data.os?.[0]?.total!==undefined,servicePriceVisible:r.data.servicos?.[0]?.valor!==undefined});}
const other=await request('/api/estado','mecanico','GET',undefined,{'x-tenant-id':'other_office'}); // headers overridden below for true cross-tenant
const cross=await fetch(base+'/api/estado',{headers:{Authorization:'Basic '+Buffer.from('audit_mecanico:'+pass).toString('base64'),'x-tenant-id':'other_office'}});results.push({case:'cross_tenant',status:cross.status});
for(const role of ['atendente','mecanico'])for(const p of ['/api/backup/listar','/api/financeiro/dashboard','/api/pecas']){const r=await request(p,role);results.push({case:'role_endpoint',role,path:p,status:r.status});}
const mechanicPayload=(await request('/api/estado','mecanico')).data;mechanicPayload.os=[];mechanicPayload.clientes[0].nome='UNAUTHORIZED_CHANGE';const denied=await request('/api/estado','mecanico','POST',mechanicPayload);results.push({case:'mechanic_unauthorized_changes',osCount:(await request('/api/estado','tenant_admin')).data.os.length,clientName:(await request('/api/estado','tenant_admin')).data.clientes[0].nome});results.push({case:'mechanic_state_write',status:denied.status});
let s=(await request('/api/estado','tenant_admin')).data;const csrf=await request('/api/estado','tenant_admin','POST',s,{Origin:'https://untrusted.invalid','sec-fetch-site':'cross-site'});results.push({case:'csrf',status:csrf.status});
const write=await request('/api/estado','tenant_admin','POST',s);const stale=await request('/api/estado','tenant_admin','POST',s);results.push({case:'optimistic_version',first:write.status,stale:stale.status});
const settings=await request('/api/whatsapp/config-grupos','mecanico','POST',{grupoAdminId:'audit_changed'});results.push({case:'mechanic_whatsapp_settings',status:settings.status,secretReturned:settings.data?.cfg?.apiKeyExterna==='AUDIT_CANARY_SECRET',stored:(await request('/api/whatsapp/config-grupos','tenant_admin')).data.grupoAdminId==='audit_changed'});
const timings=[];for(let i=0;i<20;i++)timings.push((await request('/api/estado','tenant_admin')).ms);timings.sort((a,b)=>a-b);results.push({case:'authenticated_read_latency',samples:20,p50:timings[10],p95:timings[18]});
try{const puppeteer=require('puppeteer');browser=await puppeteer.launch({headless:true,args:['--no-sandbox']});const page=await browser.newPage();await page.setViewport({width:1920,height:1080});await page.authenticate({username:'audit_tenant_admin',password:pass});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(base+'/?tenant='+tenant,{waitUntil:'networkidle0',timeout:20000});await new Promise(r=>setTimeout(r,1000));results.push({case:'browser_boot',title:await page.title(),app:await page.$('#app')!==null,nav:await page.$('#nav')!==null,errors});const tabs=await page.$$eval('button[data-act="ir"][data-v]',els=>[...new Set(els.map(e=>e.getAttribute('data-v')))]);for(const tab of tabs){try{await page.click(`button[data-act="ir"][data-v="${tab}"]`);await new Promise(r=>setTimeout(r,700));results.push({case:'browser_tab',tab,appTextLength:await page.$eval('#app',e=>e.textContent.length),actualView:await page.evaluate(()=>window.S?.ui?.view)});}catch(e){results.push({case:'browser_tab',tab,error:e.message.slice(0,120)});}}await page.reload({waitUntil:'networkidle0'});await page.click('button[data-act="ir"][data-v="cadastros"]');await new Promise(r=>setTimeout(r,800));results.push({case:'browser_cadastros_view',view:await page.evaluate(()=>window.S?.ui?.view),newClientButton:!!await page.$('button[data-act="novo-cad"][data-t="cliente"]')});
try{await page.click('button[data-act="novo-cad"][data-t="cliente"]');await new Promise(r=>setTimeout(r,250));await page.type('input[data-act="rc"][data-c="nome"]','Cliente Teste Auditoria');await page.click('button[data-act="salvar-cad"]');await new Promise(r=>setTimeout(r,1800));const saved=(await request('/api/estado','tenant_admin')).data.clientes.some(c=>c.nome==='Cliente Teste Auditoria');results.push({case:'browser_create_client_persisted',saved});}catch(e){results.push({case:'browser_create_client_error',error:e.message.slice(0,160)});}
results.push({case:'browser_final_errors',errors});}catch(e){results.push({case:'browser_unavailable',error:e.message.slice(0,250)});}
}finally{if(browser)await browser.close();child.kill();await new Promise(r=>setTimeout(r,400));fs.writeFileSync('http-audit-deep-results.json',JSON.stringify(results,null,2));fs.writeFileSync('http-audit-deep-server.log',logs);console.log(JSON.stringify(results,null,2));}
})().catch(e=>{console.error(e);process.exitCode=1});

