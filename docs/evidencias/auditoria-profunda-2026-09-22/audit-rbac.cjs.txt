'use strict';
const fs=require('fs'),path=require('path'),os=require('os'),net=require('net'),{spawn}=require('child_process');
const tmp=fs.mkdtempSync(path.join(os.tmpdir(),'patio-rbac-audit-'));process.env.DB_PATH=path.join(tmp,'test.db');process.env.NODE_ENV='test';process.env.DISABLE_INTEGRATIONS='true';process.env.DISABLE_WHATSAPP='true';process.env.UPLOAD_DIR=path.join(tmp,'uploads');
const password='OnlyForIsolatedAudit#3518';for(const role of ['GESTOR','ATENDENTE','MECANICO'])process.env['PILOTO_'+role+'_SENHA']=password;
(async()=>{const db=require('./db'),u=require('./lib/auth/userRepository'),repo=require('./lib/repository/stateRepository');await db.initDB();for(const [name,role] of [['gestor','tenant_admin'],['atendente','atendente'],['mecanico','mecanico']])await u.createUser({username:name+'@oficina.com.br',password,role,tenantId:'oficina_piloto_01'});const state=repo.getDefaultState();state.os=[{id:'audit_os',num:1,total:100}];state.contas=[{id:'audit_account',valor:100}];await repo.persistState({tenantId:'oficina_piloto_01',role:'tenant_admin',permissions:['*']},state);await db.closeDB();const probe=net.createServer();await new Promise(r=>probe.listen(0,'127.0.0.1',r));const port=probe.address().port;await new Promise(r=>probe.close(r));const server=spawn(process.execPath,['server.js'],{env:{...process.env,PORT:String(port),HOST:'127.0.0.1'},stdio:'ignore',windowsHide:true});try{let ready=false;for(let i=0;i<100;i++){try{if((await fetch(`http://127.0.0.1:${port}/ready`)).status===200){ready=true;break;}}catch{}await new Promise(r=>setTimeout(r,100));}if(!ready)throw Error('boot timeout');const redirect=path.join(tmp,'redirect.cjs');fs.writeFileSync(redirect,`const original=global.fetch;global.fetch=(url,options)=>{const u=new URL(url);if(u.hostname==='127.0.0.1'&&u.port==='3000')u.port='${port}';return original(u,options)};`);const child=spawn(process.execPath,['--require',redirect,'--test','--test-timeout=30000','tests/rbac_permissions_audit.test.cjs'],{env:process.env,stdio:['ignore','pipe','pipe'],windowsHide:true});let out='';child.stdout.on('data',d=>out+=d);child.stderr.on('data',d=>out+=d);const code=await new Promise(r=>child.on('exit',r));fs.writeFileSync('rbac-isolated-audit.txt',out);console.log(out);process.exitCode=code;}finally{server.kill();}})().catch(e=>{console.error(e);process.exitCode=1});
